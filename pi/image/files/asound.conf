pcm.!default {
    type asym
    playback.pcm "plughw:0,0"
    capture.pcm "plug_capture"
}
pcm.plug_capture {
    type plug
    slave.pcm "hw:sndrpigooglevoi,0"
}
ctl.!default {
    type hw
    card 0
}
