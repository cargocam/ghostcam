# Disable NetworkManager connectivity checking.
# NM's connectivity checks cause the cellular modem to cycle (disconnect/reconnect)
# every 10-15s when WiFi is down, disrupting cellular streaming.
[connectivity]
enabled=false
