# Build artifacts
/ghostcam-camera
/ghostcam-server
# `go build ./camera` / `go build ./server` drop a binary named after the
# directory into the same directory.
/camera/camera
/server/server

# UI
ui/node_modules/
ui/build/
ui/.svelte-kit/

# Environment
.env

# OS
.DS_Store
/ghostcam-camera-*

# Test fixtures (generated on demand)
docker/test-loop.mp4
.pi.env
